stage: final

train:
  epochs: 10
  eval:
    strategy: per_epoch

hpo:
  mode: hierarchical
  grid_power: 3
  lr_grid:
    min_exp: -6
    max_exp: -3
    points: 15
  warmup_grid: [0.0, 0.02, 0.04, 0.06, 0.08, 0.1]

seeds:
  preeval: [12, 24, 36]
  final: [2, 3, 5, 7, 11]
